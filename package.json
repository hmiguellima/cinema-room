{
  "name": "cinema-room",
  "version": "1.0.0",
  "description": "Cinema Room VR",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build:client": "pnpm --filter @cinema-room/client build",
    "build:server": "pnpm --filter @cinema-room/server build",
    "build:common": "pnpm --filter @cinema-room/common build",
    "watch": "pnpm --filter @cinema-room/client watch",
    "start": "pnpm --filter @cinema-room/server start"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "24.x"
  }
}

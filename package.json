{
  "name": "cinema-room",
  "version": "1.0.0",
  "description": "Cinema Room VR",
  "private": true,
  "scripts": {
    "build:client": "pnpm --filter @cinema-room/client build",
    "dev": "concurrently \"pnpm --filter @cinema-room/client build:dev\" \"pnpm --filter @cinema-room/server start\"",
    "start": "pnpm --filter @cinema-room/server start"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "24.x",
    "npm": "please use pnpm",
    "pnpm": ">= 11.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
